<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="a portfolio site">
    <meta name="author" content="Brook Riggio and Ivan Storck">

    <title>Code Fellows Rails Portfolio</title>

    <%= stylesheet_link_tag    "application", :media => "all" %>
    <%= javascript_include_tag "application" %>
    <%= csrf_meta_tags %>
  </head>

  <body>
    <%= render "shared/nav" %>
    <div class="section">
      <div class="container">
        <div id="flash" class="row">
          <% flash.each do |key, value| %>
            <div class="<%= flash_class(key) %>">
              <%= value %>
            </div>
          <% end %>
        </div> <!-- /.row -->
      </div><!-- /.container -->
    </div><!-- /.section -->

    <div id="main" class="section-colored">
      <div class="container">
        <div class="row">
          <div class="col-lg-12">
            <%= yield %>
            <hr>
          </div>
        </div><!-- /.row -->
      </div><!-- /.container -->
    </div><!-- /.section-colored -->

    <%= render "shared/footer" %>

  </body>
</html>

